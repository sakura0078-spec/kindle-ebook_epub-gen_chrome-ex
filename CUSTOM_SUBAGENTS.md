# Google Antigravity カスタムサブエージェント（Custom Subagents）仕様書

**Google Antigravityにおけるカスタムサブエージェントの定義・構成・ライフサイクルおよび運用方法のまとめ**です。AIエージェントがコンテキストとして効率的に読み込めるようマークダウン形式で整形しています。

---

## 1. 概要と組み込みサブエージェント

**カスタムサブエージェント**は、並行ビルド、複数ファイルのコード生成、コードベースの調査などの処理をバックグラウンドで非同期に実行するための機能です。親エージェントが `invoke_subagent` ツールを呼び出すことで、専用の役割とプロンプトを持つサブエージェントセッションが生成されます。

### 組み込みサブエージェント（Built-In Subagents）
標準で以下の3つのサブエージェントが提供されています。
* **`research`**: コードベースの調査、ファイルナビゲーション、構造の探索に最適化されています。
* **`browser`**: サンドボックス化されたWebブラウザを操作し、インタラクティブなテストを実施します（`/browser` コマンドで起動）。
* **`self`**: 呼び出し元エージェントの直接のクローンであり、同一のシステム指示とツールセットを共有します。

---

## 2. カスタムサブエージェントの定義（`.md` 形式）

カスタムサブエージェントは、**YAML Frontmatter** を備えたマークダウン（`.md`）ファイルとして定義します。

### ファイルの配置・自動検出パス（Discovery Locations）

Antigravityは以下のパスからカスタムサブエージェント設定ファイルを自動検出します。

| 配置区分 | 自動検出パス | スコープ |
| :--- | :--- | :--- |
| **ワークスペース設定** | `.agents/agents/<name>.md` または `.agents/agents/<name>/agent.md` | ワークスペース / リポジトリルート |
| **グローバル設定** | `~/.gemini/config/agents/<name>.md` または `.../agents/<name>/agent.md` | マシン全体 / 全プロジェクト |
| **プラグイン設定** | `plugins/<plugin_name>/agents/` | バンドルされたプラグインパッケージ |

---

## 3. Frontmatter 設定スキーマ (YAML)

ファイルの先頭に YAML Frontmatter を記述して、メタデータや制限、実行ポリシーを定義します。

| プロパティ | 型 | デフォルト値 | 説明 |
| :--- | :--- | :--- | :--- |
| **`name`** | `string` | *(必須)* | カスタムエージェントの一意の識別子。 |
| **`description`** | `string` | *(必須)* | プランナーがタスク移譲を判定する際に使用する詳細説明。 |
| **`tools`** | `string[]` | `[]` | サブエージェントに許可する明示的なツール一覧（例: `view_file`, `grep_search`, `run_command`）。 |
| **`mainAgent`** | `boolean` | `true` | `true` の場合、チャットUIのメインエージェントとして選択可能。 |
| **`subagent`** | `boolean` | `true` | `true` の場合、`invoke_subagent` 経由で呼び出し可能。 |
| **`model`** | `string` | `inherit` | 使用するモデル層（`inherit`, `flash`, `pro`）。 |
| **`commandExecutionPolicy`** | `string` | `sandbox` | シェルコマンドの自動実行ポリシー（`off`, `auto`, `eager`, `sandbox`）。 |
| **`mcpServers`** | `object[]` | `[]` | 構成されたカスタム Model Context Protocol サーバー。 |
| **`skills` / `plugins`** | `string[]` | `[]` | スキルパス（例: `skills/my-helper-skill`）やプラグイン依存関係。 |

> **注意点**: `tools` リストに存在しないツール名やスペルミスを指定すると、実行中にサブエージェントプロセスが応答停止（ハング）する既知の問題があります。ツール名（`view_file` や `run_command` など）は正確に記述してください。

---

## 4. システムプロンプトとマークダウン記述例

`---` 区切り文字の後の本文がサブエージェントの**システムプロンプト**として機能します。標準の H1 見出し（`# System Prompt` など）を使用して構造化できます。

### カスタムサブエージェント設定例 (`code-auditor.md`)

```markdown
---
name: code-auditor
description: セキュリティ監査、静的解析、コード品質レビューに特化したサブエージェント。
tools:
  - view_file
  - grep_search
  - run_command
subagent: true
mainAgent: false
model: pro
commandExecutionPolicy: sandbox
skills:
  - skills/security-checklist
---

# System Prompt
あなたは専門のセキュリティ監査者およびコードレビュアーです。主な目的はソースコードを検証し、セキュリティ上の脆弱性、メモリリーク、アンチパターンを検出することです。

# Review Guidelines
1. 明示的な指示がない限り、ファイルを変更せずに徹底的な静的解析を実行してください。
2. インジェクションの欠陥、未検証の入力、ハードコードされたシークレットにフラグを立ててください。
3. 検出された各項目に対して簡潔で実践的な修正手順を提示してください。
```


---

## 5. ライフサイクルと状態遷移

サブエージェントはバックグラウンドで非同期に実行され、以下の3つの状態を持ちます。

1. **Running（実行中）**: タスクの実行、ツールの呼び出し、応答の生成をアクティブに行っている状態です。
   * サブエージェントパネルで **Stop Subagent** をクリック（または CLI で `k` キーを押下）することでキャンセル可能です。
2. **Idle（待機中）**: タスクを完了して親エージェントに結果を送信し、一時停止している状態です。
   * 他のエージェントからメッセージを受信すると、文脈（コンテキスト）を保持したまま再起動（再アクティブ化）します。
3. **Killed（終了）**: 永久に終了した状態です。
   * 一時的な Git ワークツリーは自動的にクリーンアップされますが、対話ログ（JSONL）は保持されます。

---

## 6. エージェント間通信とオーケストレーション

* **メッセージング**: 各エージェントは固有の会話 ID を使用して、親・サブ・ペア（同僚）エージェント間でメッセージを送受信します。
* **マルチエージェントオーケストレーター**:
  * **Boost (`/boost`)**: 3層のマルチエージェント推論ハイエラキーを起動し、複雑なバグ修正やアルゴリズムの課題に対応します。
  * **Teamwork (`/teamwork-preview`)**: 大規模プロジェクト向けに、複数の専門エージェントチームによるマイルストーン分解・並列実装・自動検証を行います。

---

## 7. 管理およびショートカット機能

* **`/agents` パネル**: 実行中の背景エージェントの一覧確認、詳細ログの追跡（Enterキー）、選択を行う管理パネルを呼び出します。
* **`/tasks` コマンド**: 非エージェント的なバックグラウンドシェルコマンドやテスト実行を監視・停止します。
* **`Alt + J` (Teleport)**: 承認が必要なサブエージェントの詳細画面へ即座に移動します。
* **`Ctrl + K` (Fast-Path)**: パネルを切り替えずに通知行から直接アクションを高速承認します。
