# Kindle to EPUB Converter: 自律開発ハーネス運用指針 (gemini.md)

このドキュメントは、Google Antigravity 2.0 環境において、人間（ユーザー）の介入を最小限に抑え、エージェント群が自律的に「実装 ⇄ 実機検証 ⇄ 修正」のループを回し続けるための基本行動規範です。

---

## 1. コア原則（Guiding Principles）

1. **ユーザー介入の最小化（Autonomous Continuity）**
   - ユーザーへの確認・質問は「初期の1回（ログイン認証）」と「全基準クリア後の最終報告」のみとする。
   - 軽微なエラーや仕様の迷いは、本ドキュメントの基準に従って自己完結で判断・修正する。
2. **客観的テスト駆動（Test-Driven Feedback）**
   - 実装の正しさは、必ず「実際のGUIブラウザ操作」および「EPUB規格・画像品質の機械的検査」を通じて検証する。
   - 推測で完了と判断せず、自動評価サブエージェントがグリーン（Pass）を出すまでループを反復する。
3. **安全第一（Fail-Safe Execution）**
   - テスト中は1冊全体（数百ページ）をいきなり回さず、まずは「3〜5ページのスモークテスト」から段階的に検証する。
   - リトライ上限（同一箇所の連続失敗5回）を設け、無限ループに陥った場合は安全に停止してログを保全する。

---

## 2. 専門サブエージェント体制と役割分担

Antigravity 2.0 のカスタムサブエージェント機能（`.agents/agents/`）を活用し、以下の3者による分業体制をとる。

```
┌─────────────────────────────────────────────────────────────┐
│              親エージェント（Autonomous Orchestrator）      │
│        gemini.md に基づき、タスク進行とループを管理         │
└──────────────┬───────────────────────────────┬──────────────┘
               │                               │
               ▼                               ▼
┌──────────────────────────────┐ 修正完了 ┌──────────────────────────────┐
│  【extension-coder】          ├───────► │  【test-evaluator】          │
│  (実装・バグ修正・コード改善) │         │  (実ブラウザ操作・E2Eテスト) │
└──────────────────────────────┘         └──────────────┬───────────────┘
                                                        │ (検証結果・ログ)
                                                        ▼
                                         ┌──────────────────────────────┐
                                         │  【qa-auditor】              │
                                         │  (EPUB規格・品質・合否判定)  │
                                         └──────────────────────────────┘
```

1. **`extension-coder`（実装・修正）**:
   - Chrome拡張機能（Manifest V3, Service Worker, content.js, sidepanel, epub-builder）の実装・改修。
   - エラーログや品質フィードバックを受け取り、ソースコードの的確なピンポイント修正を行う。
2. **`test-evaluator`（実機GUIテスト実行）**:
   - ユーザーのログイン済みプロファイル（方式C）のChromeをGUI表示で起動。
   - 実Kindle Cloud Reader上でDOM解析、本文領域認識、キーめくり、サイドパネル操作を自動実行する。
3. **`qa-auditor`（品質監査・合否判定）**:
   - 出力されたEPUBファイルのZIP構造・XML規格（OPF, Nav, XHTML）を自動検証。
   - 抽出画像に白飛び、重複、欠落がないかを検査し、厳格なPass/Fail判定と具体的改善点を返す。

---

## 3. 自律ループの実行規約（The Loop）

### ループの流れ
```
[Step 1] test-evaluator を起動し、実ブラウザでテスト走行（3〜5ページ）
   │
   ▼
[Step 2] qa-auditor を起動し、実行ログおよび出力EPUBを検査
   │
   ├─► 【全基準 Pass】 ──► フェーズ完了（耐久テストへ移行、または完成報告）
   │
   ▼ 【Fail 項目あり】
[Step 3] extension-coder を起動し、不合格項目の原因特定とコード修正
   │
   ▼ 修正完了
[Step 4] Step 1 へ戻り、再検証を実行（自律反復）
```

---

## 4. 厳格な合格基準（Exit Criteria）

以下のすべての条件を同時に満たした場合にのみ「合格（Pass）」と判定する。

| 評価項目 | 合格基準 (Pass) | 不合格基準 (Fail) |
| :--- | :--- | :--- |
| **ページ整合性** | めくったページ数と、EPUB内の画像枚数が100%一致 | ページの欠落・スキップ、または重複撮影が存在する |
| **画像品質** | 全ページに本文がくっきり写っており、白飛びなし | 読み込み途中スピナー、または真っ白な画像が含まれる |
| **トリミング精度** | Amazonのヘッダー/フッターが除外され、本文枠と一致 | 本文の端が削れている、または外側UIが写り込んでいる |
| **EPUB規格** | EPUB 3仕様（mimetype非圧縮先頭、OPF/Spine/XHTML完全準拠） | ファイル破損、マニフェスト不一致、リーダーで開けない |
| **例外ゼロ** | ブラウザコンソールおよび拡張機能に未処理例外なし | JavaScriptエラー、通信タイムアウト、メモリリーク |

---

## 5. 安全機能と緊急停止ルール
- **Escキー緊急停止**: テスト中、画面上でEscキーが押された場合は直ちにスキャンを中断する。
- **連続重複ガード**: 画面キャプチャが連続して変化しない場合、最大3回で安全に自動停止し、無限ループを防止する。
- **IndexedDBメモリ保護**: キャプチャ画像はメモリ内に保持せず、必ずIndexedDBに都度退避する。
